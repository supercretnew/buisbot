# Business Bot - Telegram AI Assistant

Умный бот-помощник для Telegram, использующий Google Gemini AI для ответов на сообщения. Поддерживает запуск нескольких аккаунтов одновременно через единый конфигурационный файл.

## ✨ Особенности

- **Мультиаккаунт поддержка** - запуск нескольких ботов из одного контейнера
- **AI-ответы** через Google Gemini с поддержкой контекста чата
- **Анализ медиа** - обработка фото, видео и голосовых сообщений
- **История сообщений** - автоматическое сохранение с приоритизацией важных
- **Whitelist система** - контроль доступа к боту по чатам
- **Docker поддержка** - простое развёртывание и масштабирование

## 🏗️ Архитектура

Проект построен по модульной архитектуре:

- `bot.py` - класс Bot с инкапсуляцией логики бота
- `main.py` - оркестратор для запуска нескольких ботов
- `ai_service.py` - работа с Gemini API
- `database.py` - управление SQLite базой
- `utils.py` - вспомогательные функции
- `add_session.py` - утилита для добавления новых аккаунтов

## 📦 Установка

### Предварительные требования

- Docker и Docker Compose
- Telegram API credentials (получите на [my.telegram.org](https://my.telegram.org))
- Google Gemini API key (получите на [aistudio.google.com](https://aistudio.google.com/apikey))

> **Примечание:** Проект использует [uv](https://docs.astral.sh/uv/) для управления зависимостями — быстрый и современный менеджер пакетов Python.

### Шаги установки

1. **Клонируйте репозиторий:**

```bash
git clone https://github.com/yourusername/buisbot.git
cd buisbot
```

2. **Создайте первую сессию:**

```bash
docker compose run --rm bots uv run python add_session.py
```

Следуйте инструкциям для входа в ваш Telegram аккаунт.

3. **Создайте `config.json`:**

На основе `config.json.example`, создайте `config.json`:

```json
[
  {
    "session_name": "my_bot",
    "api_id": 1234567,
    "api_hash": "your_api_hash",
    "bot_owner_id": 123456789,
    "database_path": "data/my_bot.db",
    "gemini_api_key": "your_gemini_api_key"
  }
]
```

**Важно:** Каждый бот теперь использует свой собственный Gemini API ключ. Это позволяет:

- Использовать разные Google Cloud проекты для разных ботов
- Управлять квотами и затратами отдельно
- Иметь полную изоляцию между ботами

4. **Запустите бота:**

```bash
docker compose up -d --build
```

## 🚀 Быстрый старт

### Локальная разработка (без Docker)

Если вы хотите запустить бота локально:

1. **Установите uv:**
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

2. **Установите зависимости:**
```bash
uv sync
```

3. **Создайте сессию:**
```bash
uv run python add_session.py
```

4. **Создайте config.json** (см. выше)

5. **Запустите бота:**
```bash
uv run python main.py
```

### Добавление нового бота

1. Запустите утилиту добавления сессии:

```bash
docker compose run --rm bots python add_session.py
```

2. Добавьте конфигурацию в `config.json`:

```json
{
  "session_name": "second_bot",
  "api_id": 7654321,
  "api_hash": "another_hash",
  "bot_owner_id": 123456789,
  "database_path": "data/second_bot.db",
  "gemini_api_key": "another_gemini_api_key"
}
```

3. Перезапустите сервис:

```bash
docker compose up -d --build
```

### Просмотр логов

```bash
docker compose logs -f bots
```

### Остановка бота

```bash
docker compose down
```

## 🎮 Команды бота

### Управление ботом

- `!enable` - активировать бота в текущем чате
- `!disable` - деактивировать бота в текущем чате
- `!test` - проверить работу бота (только в whitelist чатах)
- `!debug` - показать последние 10 сообщений из базы

### Работа с AI

- `Гемини, <запрос>` - обычный запрос к AI с историей чата
- `!Гемини <сообщение>` - пометить сообщение как важное (только владелец)
- `!думай` в тексте - использовать thinking-режим для сложных запросов

### Анализ медиа

- `!media <промпт>` - проанализировать медиафайл в ответе
  - Поддержка: фото, видео, аудио, голосовые сообщения
  - Пример: ответьте на сообщение с фото и напишите `!media что на этой картинке?`

## 📁 Структура проекта

```
buisbot/
├── main.py              # Точка входа (запуск ботов)
├── bot.py               # Класс Bot
├── ai_service.py        # Gemini API интеграция
├── database.py          # SQLite управление
├── utils.py             # Утилиты
├── add_session.py       # Утилита добавления сессий
├── config.json          # Конфигурация ботов
├── config.json.example  # Пример конфигурации
├── system_prompt.txt    # System prompt для Gemini
├── pyproject.toml       # Зависимости проекта (uv)
├── docker-compose.yml   # Docker конфигурация
├── Dockerfile           # Docker образ
└── data/                # Сессии и базы данных
    ├── *.session        # Telegram сессии
    └── *.db             # SQLite базы
```

## 🔧 Настройка

### System Prompt

Отредактируйте `system_prompt.txt` для изменения поведения AI. Изменения применятся при следующем запросе.

### База данных

Каждый бот использует отдельную SQLite базу для:

- Хранения истории сообщений
- Управления whitelist чатов
- Отслеживания важных сообщений

### Логирование

Логи содержат:

- Статус запуска ботов
- Обработку команд и запросов
- Ошибки и исключения
- Взаимодействие с Gemini API

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте, активирован ли бот в чате: `!enable`
2. Убедитесь, что запрос начинается с "Гемини,"
3. Проверьте логи: `docker compose logs -f bots`

### Ошибки сессии

Если файл сессии повреждён:

1. Удалите старую сессию: `rm data/session_name.session`
2. Создайте новую: `docker compose run --rm bots python add_session.py`

### Ошибки Gemini API

- Проверьте `gemini_api_key` в `config.json` для соответствующего бота
- Убедитесь в наличии квоты API для данного ключа
- Проверьте формат медиафайлов (поддерживаются: jpg, png, mp4, ogg, mp3)
- Убедитесь, что ключ активен и имеет доступ к Gemini API

## 🔒 Безопасность

- **Защитите `config.json`** - содержит API ключи и другие sensitive данные
- **Не коммитьте `config.json` в Git** - используйте `.gitignore`
- **Ограничьте bot_owner_id** - только владелец может помечать важные сообщения
- **Используйте whitelist** - контролируйте доступ к боту
- **Разные API ключи для разных ботов** - изолируйте квоты и управляйте затратами

## 📝 Changelog

### v2.1 (Текущая)

- 🔑 **Персональные API ключи** - каждый бот использует свой Gemini API ключ
- 🏗️ **Dependency Injection** - ai_service больше не зависит от глобального состояния
- 🔒 **Полная изоляция ботов** - независимые квоты и настройки для каждой сессии
- 📝 **Универсальная документация** - инструкции без привязки к конкретному пользователю
- ⚡ **Переход на uv** - быстрое управление зависимостями вместо poetry
- 🗑️ **Удалён .env файл** - все настройки в config.json для каждого бота

### v2.0 (Рефакторинг)

- ✨ Новая архитектура с классом Bot
- 🔄 Поддержка нескольких ботов через config.json
- 📦 Упрощённый Docker Compose (один сервис)
- 🛠️ Утилита для добавления сессий
- 📄 System prompt в отдельном файле
- 🧹 Удалён ненадёжный prevent_injection
- 🏗️ Улучшенная структура ai_service.py
- 📊 Оптимизированное хранение сообщений

### v1.0 (Исходная версия)

- Базовая функциональность бота
- Интеграция с Gemini
- Хранение истории сообщений

## 📦 Миграция с предыдущих версий

Если вы обновляетесь с v2.0 или v1.0, обязательно прочитайте [MIGRATION.md](MIGRATION.md) для пошаговых инструкций по миграции.

## 🤝 Вклад

Pull requests приветствуются! Для крупных изменений сначала откройте issue для обсуждения.

## 📄 Лицензия

[MIT](LICENSE)

## 👤 Автор

Создано [@kotazzz](https://github.com/kotazzz)

---

**Примечание:** Проект активно развивается с акцентом на архитектурную чистоту, масштабируемость и гибкость настройки.
